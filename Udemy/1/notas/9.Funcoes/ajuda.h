void mensagem();
int soma(int num1, int num2);
int multiplicacao(int num1, int num2);

//os protótipos de funções não tem a implementação das funções, tem apenas as prototipagens, a implementação está em outro arquivo.